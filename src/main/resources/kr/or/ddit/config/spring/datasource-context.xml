<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:context="http://www.springframework.org/schema/context"
	xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context-4.3.xsd">
	
	<!-- 
		1.datasource : connection pool - 필요한 것들 dirver=>driverClassName으로 할것!, url, username, password
		2.SqlSessionFactoryBean : datasource를 이용하여 mybatis 설정을 담당
		3.SqlSessionTemplate : sqlSessionFactory 객체로 부터 mybatis를 통해 sql을 실핼할 수 있는 객체(sqlSession)
	-->
	<context:property-placeholder location="classpath:kr/or/ddit/db/db.properties"/>
	
	<!-- 1 -->
	<bean id="dataSource" class="org.apache.commons.dbcp2.BasicDataSource" >
		<property name="url" value="${jdbc.url}"/>
		<property name="driverClassName" value="${jdbc.driver}"/>
		<property name="username" value="${jdbc.username}"/>
		<property name="password" value="${jdbc.password}"/>
	</bean>
	
	<!-- 2 -->
	<!-- mybatisUtil 역할 -->
	<!-- 
		sqlSessionFactoryBean 객체는 객체를 생성하는 역할을 담당하는 객체 스프링 프레임워크는 스프링 빈을 생성시 객체가 factoryBean 인터페이스를 구현한 경우 
		인터페이스에 정의되어있는 getObject()메소드가 리턴하는 객체를 스프링 빈으로 등록한다. 
	-->
	<bean id="sqlSessionFactory" class="org.mybatis.spring.SqlSessionFactoryBean">
		<property name="configLocation" value="classpath:kr/or/ddit/db/mybatis-config22.xml"/>
		<property name="dataSource" ref="dataSource"/>
	</bean>
	<!-- 3 -->
	<bean id="sqlSessionTemplate" class="org.mybatis.spring.SqlSessionTemplate">
		<constructor-arg ref="sqlSessionFactory"/>
	</bean>
</beans>
